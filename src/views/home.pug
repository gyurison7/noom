doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Noom
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
        style.
            button {
                background-color: #fd9644;
                border: none;
            }
            #chat {
                width: 850px;
                display: flex none;
                justify-content: flex-end;
                flex-direction: column;
            }
            #chat ul {
                list-style-type: none;
                padding: 0;
            }
            #chat li {
                width: 200px;
                max-width: 200px;
                padding: 3px 10px;
                overflow-wrap: break-word;
                border-radius: 10px;
                margin-bottom: 12px;
                background-color: #d1d8e0;
            }
    body 
        header 
            h1 Welcome to Noom
        main 
            div#nickname
                h3 먼저 닉네임을 설정해주세요!
                form#name
                    input(placeholder="닉네임을 입력해주세요", required, type="text")
                    button 설정
            div#room
                h3 채팅방 이름을 입력해주세요! (채팅방을 생성하시거나 참여하실 수 있습니다.)
                form
                    input(placeholder="채팅방 이름을 입력해주세요", required, type="text")
                    button 입장
                h4 현재 생성된 채팅방 목록
                ul
            div#chat 
                h3
                ul
                form#msg
                    input(placeholder="메시지를 입력해주세요", required, type="text")
                    button 전송
        script(src="/socket.io/socket.io.js")
        script(src="/public/js/app.js")